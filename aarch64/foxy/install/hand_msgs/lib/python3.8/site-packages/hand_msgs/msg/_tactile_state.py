# generated from rosidl_generator_py/resource/_idl.py.em
# with input from hand_msgs:msg/TactileState.idl
# generated code does not contain a copyright notice


# Import statements for member types

# Member 'normal_force'
# Member 'tangential_force'
# Member 'direction_angle'
# Member 'approximate_value'
# Member 'tactile_state'
import array  # noqa: E402, I100

import rosidl_parser.definition  # noqa: E402, I100


class Metaclass_TactileState(type):
    """Metaclass of message 'TactileState'."""

    _CREATE_ROS_MESSAGE = None
    _CONVERT_FROM_PY = None
    _CONVERT_TO_PY = None
    _DESTROY_ROS_MESSAGE = None
    _TYPE_SUPPORT = None

    __constants = {
    }

    @classmethod
    def __import_type_support__(cls):
        try:
            from rosidl_generator_py import import_type_support
            module = import_type_support('hand_msgs')
        except ImportError:
            import logging
            import traceback
            logger = logging.getLogger(
                'hand_msgs.msg.TactileState')
            logger.debug(
                'Failed to import needed modules for type support:\n' +
                traceback.format_exc())
        else:
            cls._CREATE_ROS_MESSAGE = module.create_ros_message_msg__msg__tactile_state
            cls._CONVERT_FROM_PY = module.convert_from_py_msg__msg__tactile_state
            cls._CONVERT_TO_PY = module.convert_to_py_msg__msg__tactile_state
            cls._TYPE_SUPPORT = module.type_support_msg__msg__tactile_state
            cls._DESTROY_ROS_MESSAGE = module.destroy_ros_message_msg__msg__tactile_state

            from std_msgs.msg import Header
            if Header.__class__._TYPE_SUPPORT is None:
                Header.__class__.__import_type_support__()

    @classmethod
    def __prepare__(cls, name, bases, **kwargs):
        # list constant names here so that they appear in the help text of
        # the message class under "Data and other attributes defined here:"
        # as well as populate each message instance
        return {
        }


class TactileState(metaclass=Metaclass_TactileState):
    """Message class 'TactileState'."""

    __slots__ = [
        '_header',
        '_names',
        '_normal_force',
        '_tangential_force',
        '_direction_angle',
        '_approximate_value',
        '_tactile_state',
    ]

    _fields_and_field_types = {
        'header': 'std_msgs/Header',
        'names': 'sequence<string>',
        'normal_force': 'sequence<float>',
        'tangential_force': 'sequence<float>',
        'direction_angle': 'sequence<float>',
        'approximate_value': 'sequence<uint32>',
        'tactile_state': 'sequence<uint16>',
    }

    SLOT_TYPES = (
        rosidl_parser.definition.NamespacedType(['std_msgs', 'msg'], 'Header'),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.UnboundedString()),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.BasicType('float')),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.BasicType('float')),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.BasicType('float')),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.BasicType('uint32')),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.BasicType('uint16')),  # noqa: E501
    )

    def __init__(self, **kwargs):
        assert all('_' + key in self.__slots__ for key in kwargs.keys()), \
            'Invalid arguments passed to constructor: %s' % \
            ', '.join(sorted(k for k in kwargs.keys() if '_' + k not in self.__slots__))
        from std_msgs.msg import Header
        self.header = kwargs.get('header', Header())
        self.names = kwargs.get('names', [])
        self.normal_force = array.array('f', kwargs.get('normal_force', []))
        self.tangential_force = array.array('f', kwargs.get('tangential_force', []))
        self.direction_angle = array.array('f', kwargs.get('direction_angle', []))
        self.approximate_value = array.array('I', kwargs.get('approximate_value', []))
        self.tactile_state = array.array('H', kwargs.get('tactile_state', []))

    def __repr__(self):
        typename = self.__class__.__module__.split('.')
        typename.pop()
        typename.append(self.__class__.__name__)
        args = []
        for s, t in zip(self.__slots__, self.SLOT_TYPES):
            field = getattr(self, s)
            fieldstr = repr(field)
            # We use Python array type for fields that can be directly stored
            # in them, and "normal" sequences for everything else.  If it is
            # a type that we store in an array, strip off the 'array' portion.
            if (
                isinstance(t, rosidl_parser.definition.AbstractSequence) and
                isinstance(t.value_type, rosidl_parser.definition.BasicType) and
                t.value_type.typename in ['float', 'double', 'int8', 'uint8', 'int16', 'uint16', 'int32', 'uint32', 'int64', 'uint64']
            ):
                if len(field) == 0:
                    fieldstr = '[]'
                else:
                    assert fieldstr.startswith('array(')
                    prefix = "array('X', "
                    suffix = ')'
                    fieldstr = fieldstr[len(prefix):-len(suffix)]
            args.append(s[1:] + '=' + fieldstr)
        return '%s(%s)' % ('.'.join(typename), ', '.join(args))

    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        if self.header != other.header:
            return False
        if self.names != other.names:
            return False
        if self.normal_force != other.normal_force:
            return False
        if self.tangential_force != other.tangential_force:
            return False
        if self.direction_angle != other.direction_angle:
            return False
        if self.approximate_value != other.approximate_value:
            return False
        if self.tactile_state != other.tactile_state:
            return False
        return True

    @classmethod
    def get_fields_and_field_types(cls):
        from copy import copy
        return copy(cls._fields_and_field_types)

    @property
    def header(self):
        """Message field 'header'."""
        return self._header

    @header.setter
    def header(self, value):
        if __debug__:
            from std_msgs.msg import Header
            assert \
                isinstance(value, Header), \
                "The 'header' field must be a sub message of type 'Header'"
        self._header = value

    @property
    def names(self):
        """Message field 'names'."""
        return self._names

    @names.setter
    def names(self, value):
        if __debug__:
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 all(isinstance(v, str) for v in value) and
                 True), \
                "The 'names' field must be a set or sequence and each value of type 'str'"
        self._names = value

    @property
    def normal_force(self):
        """Message field 'normal_force'."""
        return self._normal_force

    @normal_force.setter
    def normal_force(self, value):
        if isinstance(value, array.array):
            assert value.typecode == 'f', \
                "The 'normal_force' array.array() must have the type code of 'f'"
            self._normal_force = value
            return
        if __debug__:
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 all(isinstance(v, float) for v in value) and
                 True), \
                "The 'normal_force' field must be a set or sequence and each value of type 'float'"
        self._normal_force = array.array('f', value)

    @property
    def tangential_force(self):
        """Message field 'tangential_force'."""
        return self._tangential_force

    @tangential_force.setter
    def tangential_force(self, value):
        if isinstance(value, array.array):
            assert value.typecode == 'f', \
                "The 'tangential_force' array.array() must have the type code of 'f'"
            self._tangential_force = value
            return
        if __debug__:
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 all(isinstance(v, float) for v in value) and
                 True), \
                "The 'tangential_force' field must be a set or sequence and each value of type 'float'"
        self._tangential_force = array.array('f', value)

    @property
    def direction_angle(self):
        """Message field 'direction_angle'."""
        return self._direction_angle

    @direction_angle.setter
    def direction_angle(self, value):
        if isinstance(value, array.array):
            assert value.typecode == 'f', \
                "The 'direction_angle' array.array() must have the type code of 'f'"
            self._direction_angle = value
            return
        if __debug__:
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 all(isinstance(v, float) for v in value) and
                 True), \
                "The 'direction_angle' field must be a set or sequence and each value of type 'float'"
        self._direction_angle = array.array('f', value)

    @property
    def approximate_value(self):
        """Message field 'approximate_value'."""
        return self._approximate_value

    @approximate_value.setter
    def approximate_value(self, value):
        if isinstance(value, array.array):
            assert value.typecode == 'I', \
                "The 'approximate_value' array.array() must have the type code of 'I'"
            self._approximate_value = value
            return
        if __debug__:
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 all(isinstance(v, int) for v in value) and
                 all(val >= 0 and val < 4294967296 for val in value)), \
                "The 'approximate_value' field must be a set or sequence and each value of type 'int' and each unsigned integer in [0, 4294967295]"
        self._approximate_value = array.array('I', value)

    @property
    def tactile_state(self):
        """Message field 'tactile_state'."""
        return self._tactile_state

    @tactile_state.setter
    def tactile_state(self, value):
        if isinstance(value, array.array):
            assert value.typecode == 'H', \
                "The 'tactile_state' array.array() must have the type code of 'H'"
            self._tactile_state = value
            return
        if __debug__:
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 all(isinstance(v, int) for v in value) and
                 all(val >= 0 and val < 65536 for val in value)), \
                "The 'tactile_state' field must be a set or sequence and each value of type 'int' and each unsigned integer in [0, 65535]"
        self._tactile_state = array.array('H', value)
